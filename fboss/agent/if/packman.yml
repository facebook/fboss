# Basic configuration file for RPM deployment
# Initially generated by contbuild-and-deploy
#
# See dex for additional options:
# https://our.intern.facebook.com/intern/dex/packman/

packages:
  ctrl-FbossCtrl-pyremote:
    packager: oncall+fboss@xmail.facebook.com
    rules:
      fbmake:fboss/agent/if:ctrl-FbossCtrl-pyremote:
        fboss/agent/if/ctrl-FbossCtrl-pyremote.par:
          path:
          - /usr/local/bin/ctrl-FbossCtrl-pyremote
    summary: remote control for fboss agent prodage
