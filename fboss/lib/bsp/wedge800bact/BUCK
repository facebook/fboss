load("@fbcode_macros//build_defs:cpp_library.bzl", "cpp_library")

oncall("fboss_optics_phy")

cpp_library(
    # @autodeps-skip BspPlatformMapping.h is owned by both :bsp_platform_mapping_lib
    # (acyclic base) and :bsp_core (aggregates platform bsps); autodeps picks :bsp_core
    # -> cycle. Keep the acyclic :bsp_platform_mapping_lib dep.
    name = "wedge800bact_bsp",
    srcs = [
        "Wedge800BACTBspPlatformMapping.cpp",
    ],
    headers = [
        "Wedge800BACTBspPlatformMapping.h",
    ],
    deps = ["//thrift/lib/cpp2/protocol:protocol"],
    exported_deps = [
        "//fboss/lib/bsp:bsp_platform_mapping_lib",
    ],
)
