# Pulls sai-device-nodes.service into the boot transaction without anyone
# having to run `systemctl enable`, which needs a writable /etc and a %post
# scriptlet the SDK drops do not all carry.
[Unit]
Wants=sai-device-nodes.service
