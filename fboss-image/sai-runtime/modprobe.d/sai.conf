# linux_ngbde sizes its DMA pool at load time and the SDK expects 128 MB. The
# previous loader passed this on the insmod command line; modprobe takes it
# from here instead.
options linux_ngbde dma_size=128
